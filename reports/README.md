# Reports

After running `bfsi-rbi eval`, this directory contains:

- `results.jsonl` — raw per-case results (JSONL)
- `eval_report.md` — aggregate Markdown report with metrics tables

The `.gitignore` excludes everything except this README and `.gitkeep`.
